<div class="row">
    <div class="panel panel-primary" style="padding: 0; margin: 3px;">
        <div class="panel-heading">
            Project $project.name members:
            <button style="padding: 0" type="button" class="btn btn-info pull-right" data-toggle="modal" data-target="#projectMembershipCreationModal">
                Add member
            </button>
        </div>
        <div class="panel-body">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Name</th>
                    <th>Surname</th>
                    <th>Role</th>
                    <th>Email</th>
                    <th>Login</th>
                    <th>Remove</th>
                </tr>
                </thead>
                <tbody>
                    #foreach($membership in $project.memberships)
                    <tr>
                        <td>$membership.user.name</td>
                        <td>$membership.user.surname</td>
                        <td>$membership.projectRole</td>
                        <td>$membership.user.email</td>
                        <td>$membership.user.login</td>
                        <td>
                            #if($isUserOwner)
                                <form action="/projectMembershipRemoval" method="post">
                                    <input type="hidden" id="projectName" name="projectName" value="$project.name">
                                    <input type="hidden" id="userName" name="userName" value="$membership.user.name">
                                    <button style="padding: 0" type="submit" class="btn btn-danger">Remove</button>
                                </form>
                            #else
                                ---
                            #end

                        </td>
                    </tr>
                    #end
                </tbody>
            </table>
        </div>
    </div>

    #parse("modals/projectMembershipCreationModal.vm")
</div>